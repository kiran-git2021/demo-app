server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  # ✅ Serve existing files directly
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: disable nginx default 404 page
  error_page 404 /index.html;
}
